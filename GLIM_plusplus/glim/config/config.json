{
  /*** Global Configuration ***/
  "global": {
    "config_path": "",
    "config_ros": "config_ros.json",
    "config_logging": "config_logging.json",
    "config_viewer": "config_viewer.json",
    "config_sensors": "config_sensors.json",
    "config_preprocess": "config_preprocess.json",
    "config_odometry": "config_odometry_gpu.json",
    "config_sub_mapping": "config_sub_mapping_gpu.json",
    // P4#2 (2026-07) — DENSE LOCALIZATION-MAP profile, opt-in on
    // the Hitch dome until Robin W mapping density/CPU is validated. Derive the
    // dense variants from THIS platform's preprocess/sub-mapping configs before
    // enabling (see GLIM_plusplus/README.md, "Dense localization-map profile"),
    // then swap the two lines above to the *_dense_map.json files and
    // re-baseline the GICP fitness floor with
    // GICP_plusplus/scripts/analyze_scan_debug_log.py.
    "config_global_mapping": "config_global_mapping_gpu.json",
    "config_gnss_global": "config_gnss_global.json"
  }
}
